# specify path to brixcoin.conf or leave blank
# default is the same as BrixcoinCore
#brixcoin_conf=/home/YOUREUSER/.brixcoincore/brixcoin.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
